# Environment Variables

# Email provider (optional - if using Resend, Sendgrid, etc.)
# If not configured, the contact form uses a mailto: fallback.
# RESEND_API_KEY=your_key_here

# Site URL (used for canonical URLs and OG metadata)
NEXT_PUBLIC_SITE_URL=https://prashantsinha.dev
